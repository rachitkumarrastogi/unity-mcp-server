# Unity MCP Server Release v1.0.0

**Release date:** (see [CHANGELOG](../../CHANGELOG.md))

Initial release of the Unity MCP server.

## Summary of changes

### Added

- **Initial release:** MCP server for Unity projects (read-only, no Unity Editor required).
- **Core tools:** Project info, build scenes, agent docs (e.g. README, AGENT.md).

---

**Install:** `npx unity-mcp-server` or from [npm](https://www.npmjs.com/package/unity-mcp-server) / [MCP Registry](https://registry.modelcontextprotocol.io/?q=unity-mcp-server).
